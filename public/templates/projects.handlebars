<!-- Work -->
<div class="work">
    <div class="row">
        {{#projects}}
        <section class="four columns">
            <a href="{{link}}" target="_blank" title="{{title}}">
                <img src="{{thumbnail}}">
                <h2>{{title}}</h2>
                <p>{{description}}</p>
            </a>
        </section>
        {{/projects}}
    </div>
</div>
